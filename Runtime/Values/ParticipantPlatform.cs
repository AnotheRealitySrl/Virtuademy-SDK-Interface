namespace Virtuademy.SDK.Interface
{
    /// <summary>Which kind of client a participant is connected from.</summary>
    public enum ParticipantPlatform
    {
        /// <summary>Not reported.</summary>
        None = 0,

        WebGL = 1,

        VR = 2,
    }
}
